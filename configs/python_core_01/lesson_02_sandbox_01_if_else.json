{
  "title": "Lesson 02: Допиши условие if/else",
  "lesson": "python_core_02",
  "code": "# Допиши функцию проверки возраста\ndef can_drive(age):\n    if age >= ???:  # Исправь!\n        return True\n    else:\n        return False\n\n# Тесты\nassert can_drive(18) == True, \"18 лет — можно водить\"\nassert can_drive(17) == False, \"17 лет — нельзя водить\"\nassert can_drive(25) == True, \"25 лет — можно водить\"",
  "expected_fixes": [
    {"pattern": "age >= 18", "test": "can_drive(18) == True"}
  ],
  "hints": [
    "Водить можно с 18 лет"
  ],
  "success_message": "🚗 Отлично! Теперь функция правильно проверяет возраст!"
}
