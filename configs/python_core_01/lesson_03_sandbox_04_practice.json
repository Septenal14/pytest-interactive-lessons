{
  "title": "Lesson 03: Practice - Complex Function",
  "lesson": "python_core_03",
  "code": "# Функция проверки возраста для покупки\ndef can_buy_alcohol(age, country=\"USA\"):\n    if country == \"USA\":\n        min_age = ???  # В США с 21\n    else:\n        min_age = ???  # В других странах с 18\n    \n    if age >= min_age:\n        return ???  # Можно покупать\n    else:\n        return ???  # Нельзя покупать\n\n# Тесты\nassert can_buy_alcohol(22, \"USA\") == True\nassert can_buy_alcohol(20, \"USA\") == False\nassert can_buy_alcohol(19, \"UK\") == True\nassert can_buy_alcohol(17, \"UK\") == False",
  "expected_fixes": [
    {"pattern": "min_age = 21", "test": "USA age limit"},
    {"pattern": "min_age = 18", "test": "other countries limit"},
    {"pattern": "return True", "test": "can buy"},
    {"pattern": "return False", "test": "cannot buy"}
  ],
  "hints": [
    "В США можно с 21 года: min_age = 21",
    "В других странах с 18: min_age = 18",
    "Если возраст достаточный: return True",
    "Если недостаточный: return False"
  ],
  "success_message": "🍺 Отлично! Ты создал функцию с условиями и параметрами по умолчанию!"
}
