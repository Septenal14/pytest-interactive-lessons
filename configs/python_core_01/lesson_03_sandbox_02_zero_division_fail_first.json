{
  "title": "Найди ошибку: ZeroDivisionError",
  "lesson": "python_core_03",
  "fail_first": true,
  "initial_error": {
    "type": "ZeroDivisionError",
    "message": "division by zero",
    "line": 3,
    "explanation": "Нельзя делить на 0! Добавь проверку перед делением."
  },
  "code": "# Запусти — увидишь ошибку!\ndef calculate_average(total, count):\n    return total / count  # ❌ ОШИБКА если count = 0!\n\nresult = calculate_average(100, 0)\n\n# Исправь функцию — добавь проверку:\nassert result == 0  # Если count=0, возвращаем 0",
  "expected_fixes": [
    {"pattern": "if count == 0:", "test": "adds zero check"},
    {"pattern": "count != 0", "test": "adds zero check condition"}
  ],
  "hints": [
    "Добавь проверку: if count == 0: return 0",
    "Проверяй деление на 0 ПЕРЕД операцией деления"
  ],
  "success_message": "🛡️ Красава! Ты освоил defensive programming! Всегда проверяй деление на 0!"
}
