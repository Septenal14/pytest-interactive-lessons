{
  "title": "Lesson 01: Исправь тип данных",
  "lesson": "python_core_01",
  "code": "# Исправь типы данных\nname = \"Alice\"\nage = \"25\"  # ❌ Должно быть число!\nsalary = \"50000\"  # ❌ Должно быть число!\n\n# Тесты\nassert type(name) == str, \"name должно быть строкой\"\nassert type(age) == int, \"age должно быть числом\"\nassert type(salary) == int, \"salary должно быть числом\"",
  "expected_fixes": [
    {"pattern": "age = 25", "test": "type(age) == int"},
    {"pattern": "salary = 50000", "test": "type(salary) == int"}
  ],
  "hints": [
    "Убери кавычки вокруг 25",
    "Убери кавычки вокруг 50000"
  ],
  "success_message": "🎉 Отлично! Теперь возраст и зарплата — это числа, а не строки!"
}
